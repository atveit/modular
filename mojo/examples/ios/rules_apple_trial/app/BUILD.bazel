load("@rules_apple//apple:ios.bzl", "ios_application")
load("@rules_cc//cc:defs.bzl", "cc_import", "cc_library")
load("@build_bazel_rules_swift//swift:swift.bzl", "swift_library")

cc_import(
    name = "mojo_ios_archive",
    static_library = "libmojo_ios_static_library_smoke.a",
    hdrs = ["mojo_ios_static_library_smoke.h"],
)

cc_library(
    name = "mojo_ios_archive_consumer",
    srcs = ["ArchiveConsumer.c"],
    deps = [":mojo_ios_archive"],
)

swift_library(
    name = "trial_swift_library",
    srcs = ["TrialApp.swift"],
)

# This target is isolated from the Modular root module and deliberately has no
# Mojo archive dependency. The app probe is run only by its dedicated script.
ios_application(
    name = "trial_ios_application",
    bundle_id = "com.modular.mojo.ios.rulestrial",
    families = ["iphone", "ipad"],
    infoplists = ["Info.plist"],
    minimum_os_version = "17.0",
    deps = [
        ":mojo_ios_archive_consumer",
        ":trial_swift_library",
    ],
)
